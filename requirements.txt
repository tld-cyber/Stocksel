Flask==3.0.3
yfinance>=0.2.40
pandas>=2.0.0
pytz
pywebview==5.3.2
pyinstaller==6.10.0

# pywebview pulls these in on macOS for its native Cocoa window. Version 12.0 was yanked from PyPI
# (bad python_requires metadata for 3.9) — pin below it so pip cannot select the yanked release.
pyobjc-core<12.0; sys_platform == "darwin"
pyobjc-framework-Cocoa<12.0; sys_platform == "darwin"
pyobjc-framework-WebKit<12.0; sys_platform == "darwin"
pyobjc-framework-Quartz<12.0; sys_platform == "darwin"
